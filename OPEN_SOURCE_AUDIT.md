# Open-source readiness audit

审查对象：通用开源版 V1.3

## 已检查与修复

1. 开源版只依赖仓库内的标准库解析器，不依赖个人知识库目录或本机固定路径。
2. 导入报告只写输入文件名，不写本机完整路径。
3. 审核桥接仅绑定 `127.0.0.1`，严格校验 Host 和 Origin；只提供审核页与受限审核接口，不提供输出目录或审核 JSON 文件下载。
4. 审核结果仍为五字段格式，并通过旁路元数据绑定候选快照；候选变更或旧结果缺少快照标记时，不会自动恢复旧选择。
5. V1.2 历史升级时会对本次输入中的对话重评一次；同主题的不同事件保留，相同事件合并并保留所有来源。
6. 同时存在基础 `conversations.json` 和分片时，两者都会处理。

## 有意保持的限制

- 候选提取仍是规则启发式；人工审核仍是必要步骤。
- 同义改写、跨对话事件推断和复杂引用归因不保证正确。
- 只支持 ChatGPT Export，不扩展其他平台、不接数据库、不提供平台化 UI。
- 不读取真实 ChatGPT Export；不自动写正式知识库或删除、移动源数据。
- 测试使用合成数据，不需要外部依赖。

## 验证记录

- 开源版：`python -B -m unittest discover -s chatgpt_import_pipeline/tests -v`，19/19 通过。
- 个人版对应代码：同一命令，18/18 通过；两版各自使用隔离临时测试数据。
- 当前开源工作树扫描未发现个人项目名称、个人路径或个人导出/审核数据。
- 发布历史已替换为仅含通用当前版本的干净历史。旧克隆、Fork 或 GitHub 缓存可能仍保留先前历史。
